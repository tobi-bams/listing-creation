import React from "react";

import classes from "./Skip.module.css";

const Skip = () => {
  return (
    <button className={classes.SkipButton} type="button">
      Skip for now
    </button>
  );
};

export default Skip;
